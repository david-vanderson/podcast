
add playback speed adjusting (via ffmpeg atempo filter)
 - if playback is too fast (2x?) need to run through the filter multiple times

need to properly support stereo audio

need to sort episode list by date

can zig-sqlite be used as a zig dependency?

replace debug messages with toasts

progress bar for downloading

need image support for thumbnails
- zigimg?  stb_image?

